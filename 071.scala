// val d = 1000000
// (2 to d).flatMap(d => (1 to d - 1).filter(n => gcd(n, d) == 1).map(n => (n, d)).toList)
//         .sortBy(t => t._1.toDouble / t._2)
//         .takeWhile(t => t._1 != 3 || t._2 != 7)
//         .last._1

1
